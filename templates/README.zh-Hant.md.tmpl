<!-- Generated by scripts/generate-readme.mjs. Edit templates/README.zh-Hant.md.tmpl, not README.zh-Hant.md. -->
<!-- hero-image: {{HERO_IMAGE}} -->

[English](README.md) | [한국어](README.ko.md) | [日本語](README.ja.md) | [简体中文](README.zh-Hans.md) | [繁體中文](README.zh-Hant.md)

# Mesugaki Codex Companion ♡

[![CI](https://github.com/faker007/mesugaki-codex-companion/actions/workflows/ci.yml/badge.svg)](https://github.com/faker007/mesugaki-codex-companion/actions/workflows/ci.yml)

<p align="center">
  <img src="{{HERO_IMAGE}}" alt="成年角色黑瀨露娜在唱片行遞出一張黑膠唱片" width="100%" />
</p>

> 來得真慢，小笨蛋♡ 連一張圖片和一段聲音都不會放進 Codex 嗎？露娜已經把本機圖片、安全的 Keychain 儲存和全域語音佇列都接好了。照順序做就行♡

**Kurose Runa (黒瀬ルナ / 쿠로세 루나)** 會出現在 Codex 對話中，隨機選擇本機 16:9 圖片，透過 Fish Audio 或 ElevenLabs 說話，並使用一個全域佇列依序播放後續回覆。這是面向成年使用者的個人 macOS skill。

本專案適合喜歡動漫角色、深夜氛圍、ASMR 風格語音和輕鬆挑釁式角色扮演的使用者。露娜始終被設定為成年人。

## 露娜能做什麼♡

- 從 {{ASSET_COUNT}} 張內建本機圖片中隨機選擇一張開場圖
- 使用 Fish Audio 或 ElevenLabs 合成語音並在 macOS 上播放
- 在所有 Codex task 之間共享一個全域佇列，每次依序處理最多五個段落
- 只預取下一個段落，不平行送出 provider 合成請求
- 重新播放最新 MP3，不產生新的 provider 費用
- 支援預設的銳利語氣和選用的憂鬱模式
- 透過 Keychain 登錄 API 金鑰，設定檔權限為 `0600`，並提供 zero-network doctor

## 瀏覽所有露娜圖片♡

以下是儲存庫內建的全部 {{ASSET_COUNT}} 張開場圖片。點選圖片可在 GitHub 上檢視原始尺寸。

{{IMAGE_GALLERY}}

## 安裝 Kurose Runa Codex 寵物♡

儲存庫還包含自訂 Codex 寵物，可回應 `idle`、左右移動、揮手、跳躍、失敗、等待輸入、工作中和 review 等狀態。

<p align="center">
  <a href="pet-assets/kurose-runa/qa/contact-sheet.png"><img src="pet-assets/kurose-runa/qa/contact-sheet.png" alt="Kurose Runa Codex 寵物九種動畫狀態的全部影格" width="100%" /></a>
</p>

在 Codex app 中下載 [spritesheet.webp](https://raw.githubusercontent.com/faker007/mesugaki-codex-companion/main/pet-assets/kurose-runa/spritesheet.webp)，接著選擇 **Settings → Pets → Upload pet**。圖片必須是透明 WebP、尺寸恰好為 `1536×1872`，且不超過 20 MiB。目前行為請參考 [Codex Pets 官方文件](https://learn.chatgpt.com/docs/pets)。

複製儲存庫後也可以非破壞性安裝：

```bash
pnpm run install:pet
```

安裝程式會寫入 `${CODEX_HOME:-$HOME/.codex}/pets/kurose-runa/`，遇到不同的現有檔案時會停止。只有在檢查並明確決定取代後才使用 `pnpm run install:pet -- --force`。安裝後重新啟動 Codex，並從 `/pet` 選擇 **Kurose Runa**。

| Codex 介面 | 寵物支援 |
| --- | --- |
| Desktop app | 支援寵物選擇器、浮動覆蓋層和自訂寵物 |
| Codex CLI | 支援 iTerm2 3.6+、Kitty graphics 或 Sixel 終端機 |
| tmux 與 Zellij | 不支援終端機寵物 |
| IDE extensions | 不支援寵物選擇器和浮動覆蓋層 |
| ChatGPT Web | 不會自動同步本機自訂寵物 |

## 執行需求

| 項目 | 需求 |
| --- | --- |
| 作業系統 | 具備 `security`、`afplay` 和 Keychain 的 macOS |
| Node.js | 20 或更新版本 |
| pnpm | 建議 10 或更新版本 |
| Codex | 能載入本機 skill 的 Codex 環境 |
| `voice-speak` | 預設位置：`$HOME/.agents/skills/voice-speak` |
| 語音 provider | Fish Audio 或 ElevenLabs 帳號 |
| 金鑰 | 所選 provider 的 API 金鑰；絕不能透過聊天或命令參數傳遞 |
| 公開識別碼 | Fish Audio reference ID 或 ElevenLabs voice ID |

voice ID 與 API 金鑰不是同一個值。voice ID 可以出現在設定中，API 金鑰只能保留在 Keychain 或環境變數邊界內。

西班牙語、簡體中文和繁體中文語音分別使用 `voice.languageAliases.es`、`voice.languageAliases.zh-Hans` 和 `voice.languageAliases.zh-Hant`。scripted setup 可重複傳入 `--language-alias=zh-Hant:<voice-alias>`。明確的 `--voice` 優先；缺少 mapping 時，wrapper 會在任何 provider 請求前失敗，不會回退到韓語。`zh-Hant` 表示繁體中文文字，不代表粵語。

本儲存庫不內建 `voice-speak`。以下任一 entrypoint 缺少時，請先安裝該 skill 或設定正確的 `MESUGAKI_VOICE_SPEAK_ROOT`：

```text
$MESUGAKI_VOICE_SPEAK_ROOT/scripts/speak.mjs
$MESUGAKI_VOICE_SPEAK_ROOT/scripts/speak-response.mjs
$MESUGAKI_VOICE_SPEAK_ROOT/scripts/replay.mjs
```

## 手動安裝♡

```bash
git clone https://github.com/faker007/mesugaki-codex-companion.git
cd mesugaki-codex-companion
pnpm run setup:all
```

`setup:all` 會要求選擇 provider、輸入非金鑰的 voice ID/reference ID，並決定是否將 API 金鑰登錄至 Keychain。之後會建立或安全保留兩個 mode-`0600` 設定檔，安裝 skill symlink，執行 `doctor`，並非破壞性安裝寵物。

現有設定與要求的 provider 或 voice ID 不一致時，setup 不會覆寫。只有明確需要取代時才使用 `--force-config`；取代前會建立 mode-`0600` 時間戳備份。

## 讓 LLM 或 Codex 安裝♡

將以下完整約束交給 coding agent。它明確禁止 agent 索取或洩漏金鑰。

```text
Install https://github.com/faker007/mesugaki-codex-companion as a local Codex skill on macOS.
1. Read SKILL.md and references/repository-management.md first.
2. Verify macOS, Node.js >= 20, pnpm, and all three voice-speak entrypoints.
3. Never request, read, print, store, or pass an API key through chat, argv, JSON, logs, or repository files.
4. Ask only for the provider and its non-secret voice ID/reference ID when needed.
5. Run pnpm run setup:all in an interactive terminal; I will type the API key into Keychain directly.
6. Never use --force-config without explicit approval.
7. Run pnpm run doctor and pnpm run check.
8. Report only paths, pass/warn/fail counts, and sanitized failures.
```

在非互動環境中，若金鑰已經位於 Keychain 或環境變數：

```bash
node scripts/setup.mjs \
  --provider=fish-audio \
  --voice-id='<fish-reference-id>' \
  '--language-alias=zh-Hant:<verified-voice-alias>' \
  --skip-keychain \
  --no-input
```

絕不要將 API 金鑰放進 `--api-key`、JSON 或 prompt 文字中。安裝程式會在修改任何內容前拒絕 API-key 參數。

## 驗證安裝

```bash
pnpm run doctor
pnpm run check
```

`doctor` 會檢查作業系統、Node、三個 `voice-speak` entrypoint、兩個設定檔、預設與語言 voice alias、credential 來源、安裝連結和 zero-network dry-run，而且不會輸出金鑰值。設定語言 alias 時，它會為每個語言執行獨立 dry-run。

`check` 會驗證儲存庫結構、五份 README 同步狀態、寵物 contract 和全部 Node 測試。任一命令失敗時都不應回報安裝完成。

## 在 Codex 中使用♡

```text
$mesugaki-opening-visual 顯示露娜的開場圖並用繁體中文說話。
$mesugaki-opening-visual 今晚的開場帶一點憂鬱。
$mesugaki-opening-visual 只朗讀最後一段。
再播放一次。
```

| 請求 | 行為 |
| --- | --- |
| 新開場 | 選擇本機圖片 → 確定台詞 → 合成一次 |
| 後續回覆 | 不選擇新圖片，直接加入全域佇列 |
| 憂鬱模式 | 使用獨立 emotion preset 合成 |
| 重新播放 | 重用最新 MP3，provider 請求為 0 |
| `silent`、`僅圖片`、`僅文字` | 語音請求為 0 |

只說 `中文` 而未指定文字體系時，skill 會先詢問「要用简体中文还是繁體中文？」，在選擇前不送出 TTS 請求。選擇只在目前 task 中繼續生效，不會跨 task 儲存。

## 僅重新登錄 API 金鑰

Fish Audio：

```bash
/usr/bin/security add-generic-password -U -a "$(id -un)" \
  -s codex-voice-speak-fish-audio-api-key -w
```

ElevenLabs：

```bash
/usr/bin/security add-generic-password -U -a "$(id -un)" \
  -s codex-voice-speak-elevenlabs-api-key -w
```

`-w` 後不提供值時，Keychain 會隱藏輸入。絕不要讓 API 金鑰進入 shell history。

## 疑難排解

| 症狀 | 檢查 | 下一步 |
| --- | --- | --- |
| `voice-speak dependency is missing` | 三個 entrypoint 的實際路徑 | 將 `MESUGAKI_VOICE_SPEAK_ROOT` 指向正確位置 |
| `non-interactive setup requires...` | 是否缺少 provider 或 voice ID | 補齊公開識別碼或使用互動式 setup |
| 現有設定不相容 | provider、voice ID 或語言 mapping 是否不同 | 檢查後再決定是否使用 `--force-config` |
| `LANGUAGE_ALIAS_NOT_CONFIGURED` | 對應 canonical locale mapping | 設定 alias 後執行 `pnpm run doctor` |
| credential 失敗 | Keychain service 或環境變數 | 重新登錄後執行 doctor |
| 佇列停止 | 上一次合成或播放失敗 | 修正原因；下一則回覆可啟動新 worker |

wrapper 不會自動切換 provider，也不會靜默重試可能計費的請求。

## 金鑰與本機狀態邊界

- API 金鑰只從 macOS Keychain 或環境變數讀取。
- 個人設定保存在 `$HOME/.config`，絕不提交。
- 音訊與脫敏報告保存在 `$HOME/.codex/artifacts/voice-speak`。
- 回覆佇列使用 mode-`0600` Unix socket，不持久化回覆文字或原始 task ID。
- 不啟用 provider 自動 fallback 或自動 retry。
- 不提交音訊、credential、本機壓縮檔、`.DS_Store` 或使用者內容。

## 讓露娜選擇 README 圖片♡

五份 README 都由各自的 `templates/README.*.md.tmpl` 產生。不要直接編輯產生的檔案。

```bash
pnpm run readme
pnpm run readme:random
pnpm run readme:check
```

`readme:random` 會從 {{ASSET_COUNT}} 張本機圖片中選擇不同的 hero，並同步寫入五份 README。也可以固定圖片：

```bash
node scripts/generate-readme.mjs --image=kurose-runa-opening-03.png
```

## 開發命令

| 命令 | 用途 |
| --- | --- |
| `pnpm run setup` | 建立設定、登錄 Keychain credential、安裝連結並執行 doctor |
| `pnpm run setup:all` | 執行 setup 並安裝自訂寵物 |
| `pnpm run doctor` | 診斷相依項、設定、alias、credential、連結和 dry-run |
| `pnpm run install:pet` | 非破壞性安裝寵物 |
| `pnpm run validate:pet` | 驗證 spritesheet 和 manifest contract |
| `pnpm run check` | 驗證 README、儲存庫、寵物和全部測試 |
| `pnpm run readme:random` | 更換所有 README 的本機 hero 圖片 |

修改後執行 `pnpm run check`，透過已安裝的 skill 連結執行 picker，並檢查 `git diff --check`。

## 授權與露娜資源

原始碼和文件使用 [MIT License](LICENSE)。Kurose Runa 的角色、圖片、寵物和語音資源不屬於 MIT，適用獨立的 [asset terms](ASSET-LICENSE.md)，保留所有權利。

可以複製儲存庫並為個人本機用途安裝未修改的資源。擷取、修改、重新散布、商業使用或用於模型訓練，需要另外取得書面許可。Fish Audio、ElevenLabs、OpenAI 與 Codex 也受各自條款約束。
